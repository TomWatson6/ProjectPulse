# Your songs

Place your own `.mp3` or `.wav` files here when running the project in Unity.

The Windows build uses `Builds/Windows/Songs` by default. You can instead enter any existing folder in Audio Lab → Your Songs → Use Folder. Songs are discovered automatically while the lab is idle, or immediately with Scan.

Your audio is local-only and ignored by Git. No songs are uploaded.
